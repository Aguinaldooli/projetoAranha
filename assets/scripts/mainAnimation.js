function addEventListenersToCards() {
    const cardElements = document.getElementById("sCard");
    console.log(cardElements);
}

document.addEventListener("DOMContentLoaded", addEventListenersToCards, false);
